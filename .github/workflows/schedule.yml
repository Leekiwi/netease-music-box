name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "*/2 * * * *"

jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn install
      - name: Update gist
        uses: ./
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          GIST_ID: 79f77e1df0b75f68f6815c7b5648230d
          USER_ID: 121473597
          USER_TOKEN: ${{ secrets.USER_TOKEN }}
