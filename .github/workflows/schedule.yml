name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "*/2 * * * *"

jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn install
      - name: Update gist
        uses: ./
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          GIST_ID: ce3eccdd64186b0429545d44826b51ea
          USER_ID: 121473597
          USER_TOKEN: ${{ secrets.USER_TOKEN }}
