name: Update gist
on:
  push:
    branches:
      - master
  schedule:
    - cron: "*/2 * * * *"

jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn install
      - name: Update gist
        uses: ./
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          GIST_ID: 08241f5298fff84bdafe57d236904d00
          USER_ID: 121473597
          USER_TOKEN: ${{ secrets.USER_TOKEN }}
